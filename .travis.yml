language: node_js
node_js:
  - 12.18.4
cache: npm

install:
  npm install

script:
  - npm lint
  - npm test:ci
  - npm run build:prod

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: master

